const initState = {
  // contracts: {
  //   ...contractData,
  //   ...getDataFromLessonList(contractData.lessonId, lessons),
  // },
}

const detailsReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default detailsReducer;